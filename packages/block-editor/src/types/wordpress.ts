import type { Block } from '@wordpress/blocks';

/**
 * Alias for the WordPress block instance type.
 * BlockInstance was renamed to Block in recent @wordpress/blocks versions.
 */
export type BlockInstance = Block;
